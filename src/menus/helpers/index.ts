import ck from "chalk";

export const divider = () => {
    console.log(ck.gray("──────────────────────────────"));
};

export const byeMessage = ck.yellow("Até logo! 👋");